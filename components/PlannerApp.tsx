'use client';

import { zodResolver } from '@hookform/resolvers/zod';
import { useForm } from 'react-hook-form';
import { ArrowLeft, BedDouble, BookOpen, CalendarDays, Check, ChevronRight, CircleDollarSign, Command, FileText, Globe2, Grip, Hotel, LayoutDashboard, Map, MapPin, Menu, Navigation, Plus, Search, Sparkles, Ticket, TrainFront, Users, Utensils, WalletCards, X } from 'lucide-react';
import { useEffect, useState } from 'react';
import { DestinationRibbon } from './DestinationRibbon';
import { TripMap } from './TripMap';
import { ItineraryView } from './ItineraryView';
import { BudgetView } from './BudgetView';
import { BookingsView } from './BookingsView';
import { DocumentsView } from './DocumentsView';
import { demoTrips, japanBookings, japanDays, japanSegments, savedPlaces } from '@/lib/demo-data';
import type { EventCategory, ItineraryEvent, Segment, Trip, TripDay } from '@/lib/types';
import { categoryTotals, generateDays } from '@/lib/travel-calculations';
import { eventSchema, tripSchema, type EventInput, type TripInput } from '@/lib/validation';

type View = 'Overview'|'Itinerary'|'Map'|'Budget'|'Bookings'|'Documents';
type Modal = 'trip'|'destination'|'event'|'command'|'travel'|null;
const nav: {label:View; icon:typeof Map}[]=[{label:'Overview',icon:LayoutDashboard},{label:'Itinerary',icon:CalendarDays},{label:'Map',icon:Map},{label:'Budget',icon:CircleDollarSign},{label:'Bookings',icon:Ticket},{label:'Documents',icon:FileText}];

const tripBackground: Record<string,string>={japan:'cover-japan',alps:'cover-alps',coast:'cover-coast'};

function Dashboard({ trips, onOpen, onNew }: { trips:Trip[]; onOpen:(trip:Trip)=>void; onNew:()=>void }) {
  const [listView,setListView]=useState(false);
  return <div className="dashboard"><header className="dashboard-header"><div><p className="eyebrow">YOUR TRAVEL STUDIO</p><h1>Where to next?</h1><p>Shape every part of the journey, then carry it with you.</p></div><button className="primary-button" onClick={onNew}><Plus size={16}/> New trip</button></header><div className="dashboard-toolbar"><div><h2>My trips</h2><span>{trips.length} journeys in motion</span></div><div className="view-toggle"><button className={!listView?'active':''} onClick={()=>setListView(false)} aria-label="Card view"><Grip size={16}/></button><button className={listView?'active':''} onClick={()=>setListView(true)} aria-label="List view"><Menu size={16}/></button></div></div><div className={listView?'trip-list-view':'dashboard-trip-grid'}>{trips.map((trip,index)=><button key={trip.id} type="button" className={`dashboard-trip-card ${tripBackground[trip.cover]??'cover-new'}`} onClick={()=>onOpen(trip)}><div className="trip-cover"><span className={`trip-status status-${trip.status.toLowerCase().replace(' ','-')}`}>{trip.status}</span><span className="cover-number">0{index+1}</span></div><div className="dashboard-trip-copy"><div><p>{trip.dates}</p><h3>{trip.name}</h3><span>{trip.duration} days · {trip.countries.join(' · ')}</span></div><ChevronRight size={18}/><p className="trip-route">{trip.route}</p><div className="dashboard-card-stats"><span><strong>A${trip.planned.toLocaleString()}</strong> / A${trip.budget.toLocaleString()}</span><span><Users size={13}/>{trip.travellers.length}</span></div><div className="progress-bar"><i style={{width:`${trip.progress}%`}}/></div><small>{trip.progress}% planned</small></div></button>)}</div><section className="dashboard-inspiration"><div><p className="eyebrow">PLANNING THOUGHT</p><h2>Give every day a little breathing room.</h2><p>The best itineraries leave space for the place to surprise you.</p></div><button onClick={()=>onOpen(trips[0])}>Continue planning Japan <ChevronRight size={15}/></button></section></div>;
}

function Overview({ trip, segments, days, activeSegment, setActiveSegment, onAddDestination, onNavigate }: { trip:Trip; segments:Segment[]; days:TripDay[]; activeSegment:string; setActiveSegment:(id:string)=>void; onAddDestination:()=>void; onNavigate:(view:View)=>void }) {
  const totals=categoryTotals(days);
  return <div className="overview-view"><div className="overview-metrics"><article><span className="metric-icon coral"><WalletCards size={18}/></span><div><small>Budget</small><strong>A${trip.planned.toLocaleString()} <em>/ A${trip.budget.toLocaleString()}</em></strong><p>A${(trip.budget-trip.planned).toLocaleString()} remaining</p></div></article><article><span className="metric-icon teal"><Ticket size={18}/></span><div><small>Bookings</small><strong>8 <em>/ 12 complete</em></strong><p>3 need your attention</p></div></article><article><span className="metric-icon olive"><Check size={18}/></span><div><small>Planning progress</small><strong>{trip.progress}%</strong><p>Accommodation 6/6 · Transport 8/10</p></div></article><article><span className="metric-icon blue"><Users size={18}/></span><div><small>Travellers</small><strong>{trip.travellers.length}</strong><p>{trip.travellers.join(', ')}</p></div></article></div><section className="overview-section ribbon-card"><div className="card-title"><div><p className="eyebrow">THE JOURNEY</p><h3>{segments.length} destinations across {trip.duration} days</h3></div><button className="quiet-button" onClick={onAddDestination}><Plus size={14}/> Add destination</button></div><DestinationRibbon segments={segments} activeId={activeSegment} onSelect={setActiveSegment}/></section><div className="overview-grid"><section className="overview-section map-card"><div className="card-title"><div><p className="eyebrow">INTERACTIVE ROUTE</p><h3>{trip.route}</h3></div><button className="quiet-button" onClick={()=>onNavigate('Map')}>Open map <ChevronRight size={13}/></button></div><TripMap segments={segments} activeId={activeSegment} onSelect={setActiveSegment} compact/><div className="route-modes"><span><TrainFront size={13}/> High-speed rail</span><span><TrainFront size={13}/> Regional rail</span><span><Navigation size={13}/> Ferry & local travel</span></div></section><section className="overview-section action-card"><div className="card-title"><div><p className="eyebrow">NEXT ACTIONS</p><h3>Keep things moving</h3></div><span className="count-pill">3 left</span></div>{[{title:'Book Kyoto hotel',meta:'Accommodation · Due 4 Sep'},{title:'Reserve Shinkansen',meta:'Transport · Opens in 8 days'},{title:'Confirm travel insurance',meta:'Documents · Before departure'}].map((action)=><button key={action.title}><span/><div><strong>{action.title}</strong><small>{action.meta}</small></div><ChevronRight size={14}/></button>)}<button className="view-all" onClick={()=>onNavigate('Bookings')}>View all planning tasks</button></section></div><div className="overview-grid lower"><section className="overview-section planning-card"><div className="card-title"><div><p className="eyebrow">BUDGET SNAPSHOT</p><h3>A${trip.planned.toLocaleString()} planned</h3></div><button className="quiet-button" onClick={()=>onNavigate('Budget')}>Full budget <ChevronRight size={13}/></button></div><div className="mini-categories">{Object.entries(totals).slice(0,4).map(([name,total])=><div key={name}><span>{name}<strong>A${total}</strong></span><i><b style={{width:`${Math.min(total/500*100,100)}%`}}/></i></div>)}</div></section><section className="overview-section notes-card"><p className="eyebrow">IMPORTANT NOTE</p><h3>Pack light for Koyasan</h3><p>Forward the larger suitcase from Tokyo to Kyoto. Take only an overnight bag for the temple stay.</p><button>Open trip notes <ChevronRight size={13}/></button></section></div></div>;
}

function AddDialog({ type, days, onClose, onCreateTrip, onCreateEvent, onCreateDestination }: { type:'trip'|'destination'|'event'; days:TripDay[]; onClose:()=>void; onCreateTrip:(input:TripInput)=>void; onCreateEvent:(input:EventInput)=>void; onCreateDestination:(name:string,country:string,startDay:number,endDay:number)=>void }) {
  const tripForm=useForm<TripInput>({resolver:zodResolver(tripSchema),defaultValues:{name:'',startDate:'2027-01-10',endDate:'2027-01-17',currency:'AUD',budget:3000,travellers:1}});
  const eventForm=useForm<EventInput>({resolver:zodResolver(eventSchema),defaultValues:{title:'',category:'Attraction',dayId:days[0]?.id??'',time:'09:00',location:'',origin:'',destination:'',transportMode:'train',provider:'',estimatedCost:0,bookingStatus:'Not Required',url:''}});
  const [destination,setDestination]=useState({name:'',country:'',startDay:1,endDay:2});
  return <div className="modal-backdrop" onMouseDown={onClose}><section className="modal-panel" role="dialog" aria-modal="true" aria-labelledby="modal-title" onMouseDown={(e)=>e.stopPropagation()}><header><div><p className="eyebrow">{type==='trip'?'NEW JOURNEY':type==='destination'?'BUILD THE ROUTE':'QUICK ADD'}</p><h2 id="modal-title">{type==='trip'?'Create a new trip':type==='destination'?'Add a destination':'Add to the itinerary'}</h2></div><button onClick={onClose} aria-label="Close"><X size={18}/></button></header>{type==='trip'&&<form onSubmit={tripForm.handleSubmit(onCreateTrip)}><label>Trip name<input {...tripForm.register('name')} placeholder="e.g. Portugal by train" autoFocus/>{tripForm.formState.errors.name&&<small>{tripForm.formState.errors.name.message}</small>}</label><div className="field-row"><label>Start date<input type="date" {...tripForm.register('startDate')}/></label><label>End date<input type="date" {...tripForm.register('endDate')}/>{tripForm.formState.errors.endDate&&<small>{tripForm.formState.errors.endDate.message}</small>}</label></div><div className="field-row"><label>Base currency<select {...tripForm.register('currency')}><option>AUD</option><option>USD</option><option>EUR</option><option>GBP</option><option>JPY</option></select></label><label>Budget<input type="number" {...tripForm.register('budget',{valueAsNumber:true})}/></label><label>Travellers<input type="number" {...tripForm.register('travellers',{valueAsNumber:true})}/></label></div><button className="submit-button" type="submit">Create trip <ChevronRight size={15}/></button></form>}{type==='destination'&&<form onSubmit={(e)=>{e.preventDefault();onCreateDestination(destination.name,destination.country,destination.startDay,destination.endDay)}}><label>Destination<input value={destination.name} onChange={(e)=>setDestination({...destination,name:e.target.value})} placeholder="e.g. Queenstown" autoFocus required/></label><label>Country<input value={destination.country} onChange={(e)=>setDestination({...destination,country:e.target.value})} placeholder="e.g. New Zealand" required/></label><div className="field-row"><label>Start day<input type="number" min="1" value={destination.startDay} onChange={(e)=>setDestination({...destination,startDay:Number(e.target.value)})}/></label><label>End day<input type="number" min={destination.startDay} value={destination.endDay} onChange={(e)=>setDestination({...destination,endDay:Number(e.target.value)})}/></label></div><button className="submit-button">Add to journey <ChevronRight size={15}/></button></form>}{type==='event'&&<form onSubmit={eventForm.handleSubmit(onCreateEvent)}><div className="category-picker">{(['Accommodation','Transport','Attraction','Food','Activity','Note'] as EventCategory[]).map((category)=>{const Icon=category==='Accommodation'?BedDouble:category==='Transport'?TrainFront:category==='Attraction'?MapPin:category==='Food'?Utensils:category==='Activity'?Sparkles:BookOpen;return <button type="button" className={eventForm.watch('category')===category?'active':''} key={category} onClick={()=>eventForm.setValue('category',category)}><Icon size={17}/><span>{category}</span></button>})}</div><label>Title<input {...eventForm.register('title')} placeholder="What are you planning?" autoFocus/>{eventForm.formState.errors.title&&<small>{eventForm.formState.errors.title.message}</small>}</label><div className="field-row"><label>Day<select {...eventForm.register('dayId')}>{days.map((day)=><option value={day.id} key={day.id}>Day {day.number} · {day.title}</option>)}</select></label><label>Time<input type="time" {...eventForm.register('time')}/></label></div>{eventForm.watch('category')==='Transport'&&<><div className="field-row"><label>Origin<input {...eventForm.register('origin')} placeholder="Tokyo Station"/></label><label>Destination<input {...eventForm.register('destination')} placeholder="Kyoto Station"/></label></div><div className="field-row"><label>Mode<select {...eventForm.register('transportMode')}><option value="flight">Flight</option><option value="high_speed_train">High-speed train</option><option value="train">Train</option><option value="bus">Bus</option><option value="rental_car">Rental car</option><option value="ferry">Ferry</option><option value="bicycle">Bicycle</option><option value="walking">Walking</option></select></label><label>Provider<input {...eventForm.register('provider')} placeholder="Provider (optional)"/></label></div></>} {eventForm.watch('category')!=='Transport'&&<label>Place or address<input {...eventForm.register('location')} placeholder="Add a place"/></label>}<div className="field-row"><label>Estimated cost<input type="number" {...eventForm.register('estimatedCost',{valueAsNumber:true})}/></label><label>Booking status<select {...eventForm.register('bookingStatus')}><option>Not Required</option><option>Researching</option><option>Shortlisted</option><option>Need to Book</option><option>Reserved</option><option>Booked</option><option>Paid</option></select></label></div><details><summary>Advanced details</summary><label>Useful link<input {...eventForm.register('url')} placeholder="https://…"/>{eventForm.formState.errors.url&&<small>{eventForm.formState.errors.url.message}</small>}</label></details><button className="submit-button">Add event <ChevronRight size={15}/></button></form>}</section></div>;
}

function CommandPalette({ onClose, onAction }: { onClose:()=>void; onAction:(modal:Modal)=>void }){const [query,setQuery]=useState('');const actions=[{label:'Add attraction',icon:MapPin,modal:'event' as Modal},{label:'Add transport',icon:TrainFront,modal:'event' as Modal},{label:'Add hotel',icon:Hotel,modal:'event' as Modal},{label:'Add destination',icon:Globe2,modal:'destination' as Modal},{label:'Open Day 7',icon:CalendarDays,modal:null},{label:'Find Shinkansen booking',icon:Search,modal:null}].filter((item)=>item.label.toLowerCase().includes(query.toLowerCase()));return <div className="modal-backdrop palette-backdrop" onMouseDown={onClose}><section className="command-palette" onMouseDown={(e)=>e.stopPropagation()}><label><Search size={18}/><input autoFocus value={query} onChange={(e)=>setQuery(e.target.value)} placeholder="Search trips, days, places and bookings…"/><kbd>ESC</kbd></label><p>QUICK ACTIONS</p>{actions.map(({label,icon:Icon,modal})=><button key={label} onClick={()=>{onClose();if(modal)onAction(modal)}}><span><Icon size={17}/></span>{label}<kbd>↵</kbd></button>)}</section></div>}

function TravelMode({ onClose }: {onClose:()=>void}){return <div className="travel-mode"><header><button onClick={onClose}><ArrowLeft size={18}/> Planning</button><span className="brand"><span className="brand-mark">R</span>Roamly</span><button aria-label="Travel menu"><Menu size={19}/></button></header><main><p className="travel-greeting">GOOD MORNING</p><h1>Kyoto</h1><p>Day 7 of 15 · Wednesday, 18 May</p><div className="travel-weather"><span>18°</span><div><strong>Clear morning</strong><small>High 24° · Light rain after 6pm</small></div></div><section><div className="travel-now"><span>NOW</span><strong>Fushimi Inari at sunrise</strong><p>27 mins away · Gates are always open</p><button><Navigation size={16}/> Directions</button></div><h2>Today</h2>{japanDays[2].events.map((event,index)=><article className={index===0?'current':''} key={event.id}><time>{event.time}</time><span/><div><small>{event.category}</small><strong>{event.title}</strong><p>{event.location}</p></div><ChevronRight size={16}/></article>)}</section><section className="next-transport"><TrainFront size={21}/><div><small>NEXT TRANSPORT · TOMORROW 07:45</small><strong>Kyoto → Osaka</strong><p>Special Rapid Service · Platform to confirm</p></div><button>View ticket</button></section></main></div>}

export function PlannerApp({ userName }: { userName:string }) {
  const [trips,setTrips]=useState(demoTrips); const [trip,setTrip]=useState<Trip|null>(null); const [view,setView]=useState<View>('Overview'); const [modal,setModal]=useState<Modal>(null); const [daysByTrip,setDaysByTrip]=useState<Record<string,TripDay[]>>({japan:japanDays}); const [segmentsByTrip,setSegmentsByTrip]=useState<Record<string,Segment[]>>({japan:japanSegments}); const [activeSegment,setActiveSegment]=useState('tokyo'); const [selectedEvent,setSelectedEvent]=useState<ItineraryEvent|null>(null);
  useEffect(()=>{const key=(event:KeyboardEvent)=>{if((event.metaKey||event.ctrlKey)&&event.key.toLowerCase()==='k'){event.preventDefault();setModal('command')}if(event.key==='Escape'){setModal(null);setSelectedEvent(null)}};window.addEventListener('keydown',key);return()=>window.removeEventListener('keydown',key)},[]);
  const onCreateTrip=(input:TripInput)=>{const id=crypto.randomUUID();const generated=generateDays(input.startDate,input.endDate);const next:Trip={id,name:input.name,dates:`${input.startDate} → ${input.endDate}`,startDate:input.startDate,endDate:input.endDate,duration:generated.length,route:'Your route is ready to take shape',countries:[],status:'Idea',budget:input.budget,planned:0,paid:0,progress:15,travellers:Array.from({length:input.travellers},(_,i)=>i===0?userName:`Traveller ${i+1}`),cover:'new'};setTrips((items)=>[next,...items]);setDaysByTrip((all)=>({...all,[id]:generated.map((day)=>({id:`${id}-day-${day.dayNumber}`,number:day.dayNumber,date:day.date,title:'Open day',segmentId:'',events:[]}))}));setSegmentsByTrip((all)=>({...all,[id]:[]}));void fetch('/api/trips',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({...input,id})});setModal(null)};
  const onCreateEvent=(input:EventInput)=>{if(!trip)return;const event:ItineraryEvent={id:crypto.randomUUID(),dayId:input.dayId,title:input.title,category:input.category,time:input.time,timePrecision:input.time?'exact':'unscheduled',location:input.location,origin:input.origin,destination:input.destination,transportMode:input.transportMode,provider:input.provider,estimatedCost:input.estimatedCost,currency:'AUD',bookingStatus:input.bookingStatus,paymentStatus:'Not Paid',links:input.url?[{title:'Open link',url:input.url,type:'Reference',description:'Added with this event'}]:[],sortOrder:99};setDaysByTrip((all)=>({...all,[trip.id]:(all[trip.id]??[]).map((day)=>day.id===input.dayId?{...day,events:[...day.events,event].map((item,index)=>({...item,sortOrder:index}))}:day)}));void fetch('/api/events',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({...event,tripId:trip.id})});setModal(null)};
  const onCreateDestination=(name:string,country:string,startDay:number,endDay:number)=>{if(!trip)return;const colors=['#b9614e','#9b7b52','#667e58','#46777b','#5d7193','#805f87'];const current=segmentsByTrip[trip.id]??[];const segment:Segment={id:crypto.randomUUID(),city:name,country,startDay,endDay,color:colors[current.length%colors.length],latitude:0,longitude:0};setSegmentsByTrip((all)=>({...all,[trip.id]:[...(all[trip.id]??[]),segment]}));setActiveSegment(segment.id);void fetch('/api/segments',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({...segment,tripId:trip.id})});setModal(null)};
  const reorder=(dayId:string,events:ItineraryEvent[])=>{if(!trip)return;setDaysByTrip((all)=>({...all,[trip.id]:(all[trip.id]??[]).map((day)=>day.id===dayId?{...day,events}:day)}));void fetch('/api/events/reorder',{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify({tripId:trip.id,dayId,items:events.map(({id,sortOrder})=>({id,sortOrder}))})})};
  if(modal==='travel') return <TravelMode onClose={()=>setModal(null)}/>;
  if(!trip) return <><AppSidebar userName={userName} active="trips" onTrips={()=>setTrip(null)} onNew={()=>setModal('trip')} onCommand={()=>setModal('command')}/><div className="main-surface"><Dashboard trips={trips} onOpen={(selected)=>{setTrip(selected);setActiveSegment((segmentsByTrip[selected.id]??[])[0]?.id??'')}} onNew={()=>setModal('trip')}/></div>{modal==='trip'&&<AddDialog type="trip" days={japanDays} onClose={()=>setModal(null)} onCreateTrip={onCreateTrip} onCreateEvent={onCreateEvent} onCreateDestination={onCreateDestination}/>} {modal==='command'&&<CommandPalette onClose={()=>setModal(null)} onAction={setModal}/>}</>;
  const shownSegments=segmentsByTrip[trip.id]??[]; const shownDays=daysByTrip[trip.id]??[];
  return <><AppSidebar userName={userName} active="trips" onTrips={()=>{setTrip(null);setView('Overview')}} onNew={()=>setModal('trip')} onCommand={()=>setModal('command')}/><div className="main-surface trip-surface"><header className={`trip-hero ${tripBackground[trip.cover]??'cover-new'}`}><div className="hero-shade"/><div className="trip-hero-top"><button onClick={()=>setTrip(null)}><ArrowLeft size={15}/> All trips</button><div><button onClick={()=>setModal('travel')}><Navigation size={14}/> Travel mode</button><button aria-label="Trip menu"><Menu size={16}/></button></div></div><div className="trip-hero-copy"><span className="trip-status">{trip.status}</span><h1>{trip.name}</h1><p>{trip.dates} · {trip.duration} days · {trip.travellers.length===1?'Solo trip':`${trip.travellers.length} travellers`}</p><div>{trip.countries.map((country)=><span key={country}><Globe2 size={13}/>{country}</span>)}</div></div></header><nav className="trip-tabs" aria-label="Trip sections">{nav.map(({label,icon:Icon})=><button key={label} className={view===label?'active':''} onClick={()=>setView(label)}><Icon size={15}/>{label}</button>)}<button className="quick-add" onClick={()=>setModal('event')}><Plus size={15}/> Add</button></nav><div className="trip-content">{shownSegments.length===0?<div className="beautiful-empty"><span><MapPin size={28}/></span><p className="eyebrow">A FRESH JOURNEY</p><h2>No destinations yet</h2><p>Start by adding the first place you want this trip to take you.</p><button className="primary-button" onClick={()=>setModal('destination')}><Plus size={15}/> Add destination</button></div>:<>{view==='Overview'&&<Overview trip={trip} segments={shownSegments} days={shownDays} activeSegment={activeSegment} setActiveSegment={setActiveSegment} onAddDestination={()=>setModal('destination')} onNavigate={setView}/>} {view==='Itinerary'&&<ItineraryView days={shownDays} saved={trip.id==='japan'?savedPlaces:[]} onReorder={reorder} onAdd={()=>setModal('event')} onSelectEvent={setSelectedEvent}/>} {view==='Map'&&<div className="map-view"><div className="view-title"><div><p className="eyebrow">THE FULL JOURNEY</p><h2>See the route, not just the pins</h2><p>Select a destination to focus it across the trip.</p></div><button className="primary-button" onClick={()=>setModal('destination')}><Plus size={15}/> Destination</button></div><DestinationRibbon segments={shownSegments} activeId={activeSegment} onSelect={setActiveSegment}/><div className="map-view-grid"><TripMap segments={shownSegments} activeId={activeSegment} onSelect={setActiveSegment}/><aside><p className="eyebrow">ROUTE LEGS</p>{shownSegments.slice(0,-1).map((segment,index)=><button key={segment.id} onClick={()=>setActiveSegment(shownSegments[index+1].id)}><span className="route-count">{index+1}</span><div><strong>{segment.city} → {shownSegments[index+1].city}</strong><small><TrainFront size={12}/>{index===0?'High-speed rail · 3h 05m':'Rail · Duration saved manually'}</small></div><ChevronRight size={14}/></button>)}</aside></div></div>} {view==='Budget'&&<BudgetView days={shownDays} budget={trip.budget} planned={trip.planned} paid={trip.paid}/>} {view==='Bookings'&&<BookingsView initialBookings={trip.id==='japan'?japanBookings:[]}/>} {view==='Documents'&&<DocumentsView/>}</>}</div></div><nav className="mobile-trip-nav">{nav.slice(0,5).map(({label,icon:Icon})=><button key={label} className={view===label?'active':''} onClick={()=>setView(label)}><Icon size={19}/><span>{label==='Itinerary'?'Today':label}</span></button>)}</nav>{(modal==='trip'||modal==='destination'||modal==='event')&&<AddDialog type={modal} days={shownDays} onClose={()=>setModal(null)} onCreateTrip={onCreateTrip} onCreateEvent={onCreateEvent} onCreateDestination={onCreateDestination}/>} {modal==='command'&&<CommandPalette onClose={()=>setModal(null)} onAction={setModal}/>} {selectedEvent&&<div className="detail-drawer"><button className="drawer-close" onClick={()=>setSelectedEvent(null)}><X size={18}/></button><span className="event-type">{selectedEvent.subcategory??selectedEvent.category}</span><h2>{selectedEvent.title}</h2><p>{selectedEvent.description}</p><dl><div><dt>Time</dt><dd>{selectedEvent.time??'Flexible'}</dd></div><div><dt>Booking</dt><dd>{selectedEvent.bookingStatus}</dd></div><div><dt>Estimated cost</dt><dd>A${selectedEvent.estimatedCost}</dd></div>{selectedEvent.location&&<div><dt>Location</dt><dd>{selectedEvent.location}</dd></div>}</dl><button className="primary-button">Edit event</button><button className="danger-button">Remove</button></div>}</>;
}

function AppSidebar({userName,onTrips,onNew,onCommand}:{userName:string;active:string;onTrips:()=>void;onNew:()=>void;onCommand:()=>void}){return <aside className="product-sidebar"><button className="brand" onClick={onTrips}><span className="brand-mark">R</span><span>Roamly</span></button><button className="sidebar-add" onClick={onNew}><Plus size={16}/> New trip</button><nav><button className="active" onClick={onTrips}><LayoutDashboard size={17}/>My trips</button><button><Sparkles size={17}/>Saved ideas</button><button><WalletCards size={17}/>Travel wallet</button></nav><div className="sidebar-shortcut"><button onClick={onCommand}><Command size={15}/>Quick search <kbd>⌘K</kbd></button></div><div className="sidebar-user"><span>{userName.slice(0,2).toUpperCase()}</span><div><strong>{userName}</strong><small>Personal workspace</small></div><button aria-label="Account menu"><Menu size={15}/></button></div></aside>}
