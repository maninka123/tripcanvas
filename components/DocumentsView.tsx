'use client';

import { FileCheck2, FileText, Image, LockKeyhole, Plane, Plus, ShieldCheck, Ticket } from 'lucide-react';

const documents=[{title:'Qantas e-ticket',category:'Flights',type:'PDF',size:'284 KB',icon:Plane,color:'#507b85'},{title:'Eko-in confirmation',category:'Accommodation',type:'PDF',size:'146 KB',icon:FileCheck2,color:'#a27c54'},{title:'Travel insurance policy',category:'Insurance',type:'PDF',size:'1.2 MB',icon:ShieldCheck,color:'#687e9d'},{title:'Passport copy',category:'Identity',type:'Image',size:'2.8 MB',icon:Image,color:'#825f86'}];
export function DocumentsView(){return <div className="documents-view"><div className="view-title"><div><p className="eyebrow">TRAVEL WALLET</p><h2>Your essentials, ready when you are</h2><p>Tickets, confirmations and private documents live together.</p></div><button className="primary-button"><Plus size={15}/> Upload document</button></div><div className="wallet-security"><LockKeyhole size={19}/><div><strong>Private by default</strong><p>Files are stored securely and only available to travellers with access to this trip.</p></div></div><div className="document-grid">{documents.map(({title,category,type,size,icon:Icon,color})=><article key={title}><div className="document-preview" style={{'--doc-color':color} as React.CSSProperties}><Icon size={28}/><span>{type}</span></div><div><small>{category}</small><strong>{title}</strong><p><FileText size={12}/>{type} · {size}</p></div><button aria-label={`Open ${title}`}>›</button></article>)}<button className="document-empty"><span><Ticket size={23}/></span><strong>Add a ticket or confirmation</strong><p>PDF or image up to 20 MB</p></button></div></div>}
